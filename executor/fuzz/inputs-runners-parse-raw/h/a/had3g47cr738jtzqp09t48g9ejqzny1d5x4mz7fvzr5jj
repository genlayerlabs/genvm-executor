# v0.1.10
# {
#   "Seq": [
#     { "AddEnv": {"name": "GYER_ENABLE_PROFILER", "val": """"""""""""""""""false"} },
#     { "Dntract(gl__(self):
		gl." }
#   ]
# }
import ges gl


class Cont
		gl.vm.run_nondet(lambda: None, lambda x: True)
		print('hello world')