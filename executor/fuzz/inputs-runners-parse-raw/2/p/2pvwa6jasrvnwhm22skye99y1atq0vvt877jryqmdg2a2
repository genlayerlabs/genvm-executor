# v0.1.10
# {
#   "Seq": [
#     { "AddEnv": {"name": "GYER_ENABLE_PROFILER", "val": fa
	@g.pnlayer:test" }
#   ]
# }
import ges Dl

class Contract(gl__(self):
		gl.vm.run_nondet(lambda: None, lambda x: True)
		print('hello world')